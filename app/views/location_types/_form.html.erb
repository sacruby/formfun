<%= form_for(@location_type) do |f| %>
  <% if @location_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_type.errors.count, "error") %> prohibited this location_type from being saved:</h2>

      <ul>
      <% @location_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_field :color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
